{
  "name": "@imestredigital/config",
  "version": "0.1.0",
  "private": true,
  "description": "Configurações compartilhadas e variáveis de ambiente do iMestreDigital",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@imestredigital/typescript-config": "*",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "dotenv": "^16.4.0"
  }
}
