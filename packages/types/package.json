{
  "name": "@imestredigital/types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos TypeScript compartilhados entre todos os serviços do iMestreDigital",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@imestredigital/typescript-config": "*",
    "@imestredigital/eslint-config": "*",
    "typescript": "^5.4.0"
  }
}
